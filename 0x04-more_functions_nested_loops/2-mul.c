#include "main.h"
/**
 * mul - this function multiplies 2 values and returns the result
 *
 * @a: first value
 * @b: second value
 *
 * Return: the result of the multiplication process
 */
int mul(int a, int b)
{
	return (a * b);
}
